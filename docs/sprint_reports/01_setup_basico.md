# Sprint 01 — Setup básico

**Date:** 2026-04-21
**Step (Parte K):** 1 — Setup básico
**Status:** ✅ Complete

---

## What was done

Bootstrapped the Phase 0 project skeleton: dependency manifest, environment template, Python version pin, package directories, and a more comprehensive `.gitignore`. Validated that `uv sync` resolves and installs the full dependency graph cleanly into `.venv/`, and that the `portfolio_thesis_engine` package imports.

This sprint contains **no business logic and no tests yet** — it only establishes the ground on which Sprints 2–14 build.

## Decisions taken

1. **`.gitignore` replaced with the spec (B.3) version** as agreed (option A). The previous file was a strict subset; the new one adds `.tox/`, `.nox/`, `.hypothesis/`, `*.egg`, `MANIFEST`, `.cache`, more virtualenv variants, and explicit `.env.local` / `.env.production` exclusions. Critical exclusions (`.env`, `data/`, `*.duckdb`, `*.sqlite`) preserved.
2. **`.env.example`**: the spec template was copied verbatim, with two intentional changes:
   - `PTE_DATA_DIR` / `PTE_BACKUP_DIR` defaults updated from the spec's placeholder `/home/user/...` to the actual deployment path `/home/portfolio/...`.
   - Added a new variable **`PTE_SMOKE_HIT_REAL_APIS=false`** (Sprint 1 setup, but used by Sprint 9 `smoke-test` per decision D in the kickoff). Default `false` so smoke tests run with mocks; opt-in real-API mode for periodic manual validation.
3. **`README.md`** rewritten more substantively than the spec's stub (B.5). Adds: a 5-storage-layer table, a section explaining the `portfolio` user hardcoding in the systemd unit (per decision G), and the dual-mode smoke-test command. Kept the spec's "Phase 0 / Phase 1" status section.
4. **`.python-version`** = `3.12` — pins the interpreter version for `uv` / pyenv consumers. Listed in the spec tree (B.1) but not given content; chose the bare minor "3.12" to match `pyproject.toml`'s `requires-python = ">=3.12"`.
5. **Empty package roots** (`src/portfolio_thesis_engine/__init__.py`, `tests/__init__.py`) created so that `uv sync` succeeds and `import portfolio_thesis_engine` works. The package `__init__.py` exposes only `__version__ = "0.1.0"` for now.
6. **`docs/sprint_reports/`** created with a `.gitkeep` so the directory exists in git from sprint 1 onward.
7. **No `LICENSE` change** — the existing MIT LICENSE from the initial commit is correct.
8. **`pyproject.toml`** copied verbatim from spec B.2. No deviations.

## Spec auto-corrections

None this sprint. (Schemas come in Sprint 3.)

## Files created / modified

```
M  .gitignore                                      (replaced with spec B.3 superset)
A  .env.example                                    (new — paths localised, smoke flag added)
A  .python-version                                 (3.12)
A  README.md                                       (substantive rewrite)
A  pyproject.toml                                  (spec B.2 verbatim)
A  src/portfolio_thesis_engine/__init__.py
A  tests/__init__.py
A  docs/sprint_reports/.gitkeep
A  docs/sprint_reports/01_setup_basico.md          (this file)
A  uv.lock                                         (generated by uv sync)
```

`.env` is **not** modified and remains outside git.

## Verification

```bash
$ uv sync --extra dev
# Resolved + installed full graph (anthropic, openai, duckdb, chromadb, sqlalchemy,
# streamlit, typer, structlog, pytest+plugins, ruff, mypy, etc.) into .venv/

$ uv run python -c "import portfolio_thesis_engine; print(portfolio_thesis_engine.__version__)"
0.1.0

$ git check-ignore -v .env
.gitignore:46:.env  .env       # confirmed: .env will not be committed
```

## Tests passing / failing + coverage

N/A this sprint — no tests exist yet. `uv run pytest` would currently fail (no tests collected). First tests land in Sprint 2.

## Problems encountered

None. Single notable observation: the resolved versions are slightly newer than the spec floors (e.g. `pydantic 2.13.3` vs floor `>=2.8`, `rich 15.0.0`, `pytest 9.0.3`). All within compatible major versions. No action needed.

## Next step

**Sprint 2 — Shared utilities.** Create `shared/config.py` (pydantic-settings loading `.env`), `shared/logging_.py` (structlog), `shared/exceptions.py`, `shared/types.py`. Add `tests/unit/test_config.py`. Coverage target for the sprint's modules: ≥80%.
